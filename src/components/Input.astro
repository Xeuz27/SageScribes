<form
    id="input"
    action="/keyword"
    class="relative mx-auto mt-[-25px] flex max-w-96 items-center justify-center"
>
    <!-- <img alt="link-icon" class="absolute left-0 my-2 ml-3 w-5" /> -->
    <input
        type="text"
        placeholder="put a keyword"
        required
        class="peer block w-full rounded-md border border-gray-200 bg-white py-2.5 pl-6 pr-12 text-sm font-medium text-black/80 caret-primary shadow-lg placeholder:text-primary/60 focus:border-stone-700 focus:text-black focus:outline-none focus:ring-0 focus:placeholder:text-primary"
    />
    <button
        type="submit"
        class="absolute inset-y-0 right-0 my-1.5 mr-1.5 flex w-10 items-center justify-center rounded border border-gray-200 text-lg font-medium leading-none text-primary/60
         hover:border-gray-500 hover:text-gray-500 peer-focus:border-gray-400 peer-focus:text-primary"
    >
        <p class="mt-[-4px]">↵</p>
    </button>
</form>
<!-- value={article.url}
            onChange={(e) => {
                setArticle({ ...article, url: e.target.value });
            }} -->
<!-- onSubmit={handleSubmit}
            src={linkIcon} -->
<style></style>
<script>
    window.addEventListener('load', () => {
        const input = document.getElementById('input');
        input?.addEventListener('submit', (event): any => {
            event.preventDefault();
        });
    });
</script>
